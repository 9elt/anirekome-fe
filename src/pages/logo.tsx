import FullLogo from "../components/logo/logo"

export default function Logo() {

  return (
    <div style={{ width: 500, marginInline: 'auto', minHeight: '100vh', display: "flex", alignItems: 'center' }}>
      <FullLogo />
    </div>
  )
}